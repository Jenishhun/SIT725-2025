<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Splitter</title>
</head>
<body>
    <h1>Expense Splitter</h1>
    
    <!-- Form to add a new expense -->
    <form action="/expenses" method="POST">
        <label for="description">Description:</label>
        <input type="text" name="description" required><br>

        <label for="amount">Amount:</label>
        <input type="number" name="amount" required><br>

        <label for="paidBy">Paid By:</label>
        <input type="text" name="paidBy" required><br>

        <label for="splitBetween">Split Between (comma separated names):</label>
        <input type="text" name="splitBetween" required><br>

        <button type="submit">Add Expense</button>
    </form>

    <!-- Display the list of expenses -->
    <h2>Expenses</h2>
    <ul>
        <% expenses.forEach(expense => { %>
            <li>
                <%= expense.description %> - $<%= expense.amount %> paid by <%= expense.paidBy %>
                (Split between: <%= expense.splitBetween.join(', ') %>)
            </li>
        <% }) %>
    </ul>

</body>
</html>
